#include "main.h"

/**
 * _isupper - check letter is uppercase or not
 * @x: The letter to be checked
 * Return: 1 for upper letter or 0 for anything else
*/
int _isupper(int x)
{
	if (x >= 45 && x <= 90)
	{
	return (1);
	}
	return (0);
}
