#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to be multiplied
 * @y: the second number to be mutilied
 * Return: the result of multiplication between x param by y param
*/

int mul(int x, int y)
{
	return (x * y);
}
